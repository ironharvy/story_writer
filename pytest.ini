[pytest]
# archive/ holds the pre-rewrite implementation; its tests import modules
# that no longer exist (cli.py, models.py, world_bible_modules.py, etc.)
# and would otherwise break collection. Keep them as documentation; don't
# run them.
norecursedirs = archive .tmp .cache
